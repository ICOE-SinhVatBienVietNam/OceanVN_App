export const routeConfig = {
    intro: {
        root: "/"
    },
    main: {
        root: "/main",
        map: "/main/map",
        discover: "/main/discover",
        camera: "/main/camera",
        quest: "/main/quest",
        moreInfo: "/main/more-info",
    },
    login: {
        root: "/login"
    },
    register: {
        root: "/register"
    }
}